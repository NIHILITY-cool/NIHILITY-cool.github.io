# 听力答案背诵自测网站（完整选项版）

这是一个纯前端网站，不需要服务器。

## 使用方式

1. 解压压缩包。
2. 双击 `index.html` 打开。
3. 背诵模式：一页只显示一篇文章，左右箭头按顺序切换；四个选项都会显示，正确选项为绿色框。
4. 自测模式：随机抽一篇文章；点击具体选项内容作答，提交后显示正确/错误和得分。

## 文件说明

- `index.html`：网页入口。
- `style.css`：页面样式。
- `app.js`：题目数据和答题逻辑。

## 小提示

可以用键盘左/右方向键切换文章。自测模式下右箭头会随机换一篇。
